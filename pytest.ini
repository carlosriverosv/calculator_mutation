[pytest]
# Restrict test discovery to your actual test directory
testpaths = tests
pythonpath = .

# Explicitly ignore mutmut execution artifacts and cache folders
norecursedirs = .mutmut-cache mutmut_cache mutants mutates .git .venv

# Optional: Ensure test files follow standard naming patterns
python_files = test_*.py *_test.py